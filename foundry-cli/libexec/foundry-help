#!/usr/bin/env bash

set -euo pipefail

cat <<'EOF'
foundry - unified operations entrypoint

Usage:
  foundry [--root PATH] [--overlay PATH] [--output human|json|yaml] <command> [args]

Commands:
  doctor
  image
  host
  converge
  zt
  state
  secret
  exec
  version
EOF
