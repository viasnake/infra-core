---
- name: Foundry convergence baseline
  hosts: all
  gather_facts: false
  tasks:
    - name: Verify ansible execution path
      ansible.builtin.debug:
        msg: "foundry converge reached target hosts"
