# Ansible Assets

This directory contains convergence playbooks for Foundry-managed hosts.

- Keep plays idempotent and re-runnable.
- Use inventory values, not hardcoded provider-specific assumptions.
- Management user baseline follows the Foundry contract (`infra`).
